fastapi==0.115.*          # 웹 API 프레임워크 (엔드포인트 정의·요청 검증·자동 문서)
uvicorn[standard]==0.32.* # ASGI 서버 (FastAPI 앱을 실제로 띄워 HTTP 요청을 받음). [standard]=성능 옵션 묶음
pydantic==2.*             # 데이터 검증·직렬화 (타입에 맞는지 확인하고 변환). FastAPI가 내부적으로 사용
pydantic-settings==2.*    # .env 파일·환경변수를 Pydantic 모델로 읽어오는 설정 로더 (F0.3에서 사용)
sqlalchemy==2.*           # ORM (파이썬 객체 ↔ DB 테이블 매핑). Slice 2에서 본격 사용
psycopg[binary]==3.*      # PostgreSQL 드라이버 (파이썬에서 Postgres에 접속). [binary]=컴파일된 버전이라 설치 간편
httpx==0.27.*             # HTTP 클라이언트 (웹에서 데이터 받아옴, 동기·비동기 모두 지원). Slice 1 수집기에서 사용
pandas==2.*               # 표(DataFrame) 형태 데이터 처리 라이브러리. 정제·필터·집계에 사용. Slice 1 정제부터
alembic==1.14.*          # DB 스키마 마이그레이션 (버전 관리). Slice 2에서 사용
redis==5.*                # Redis 클라이언트 (캐시 조회/적재). 동기 API 사용. Slice 2 캐시부터
langdetect==1.0.9        # 언어 판별 (Google language-detection 파이썬 포팅). Slice 3 한국어 2차 필터

transformers==4.*         # Hugging Face 모델 파이프라인. 감성 분석(Slice 3)에 사용
# torch==2.*                # transformers 백엔드(추론 엔진). CUDA 12.8/RTX 5060 Ti 대응 빌드 사용
# pip install torch --index-url https://download.pytorch.org/whl/cu128

scikit-learn==1.*          # 머신러닝 라이브러리( 분류(Classification), 회귀(Regression), 클러스터링, 차원 축소, 데이터 전처리, 모델 평가, 데이터 분할 )
bertopic==0.17.*          # 토픽 모델링(임베딩→UMAP→HDBSCAN→c-TF-IDF). Slice 3 토픽 분석. sentence-transformers·umap-learn·hdbscan 함께 설치됨

langchain-ollama==0.2.*   # LangChain의 Ollama 챗 모델 래퍼. F3.6 토픽 이름 생성부터. Slice 4 LLM 계층에서 정식 사용